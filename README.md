# LanDrop
LanDrop is a cross-platform CLI tool for peer-to-peer file and message sharing over a local network. It automatically discovers nearby peers and uses a custom reliable transfer protocol with user approval, packet recovery, integrity verification, and no cloud or internet dependency.
